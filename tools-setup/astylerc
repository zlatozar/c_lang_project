# Usage: astyle --options="<path to>/astylerc" --recursive "src/*.c"
# ATTENTION: It will fails if there is unsaved IDE files.
#
# NOTE: could be stored in local directory as ~/.astylerc

# or allman
--style=1tbs

--indent=spaces=2
--indent-classes
--indent-switches
--indent-preproc-define
--indent-col1-comments

--attach-namespaces
--attach-classes
--attach-inlines
--attach-extern-c
--attach-closing-while

--pad-oper
--pad-comma
--pad-header

--align-pointer=type
--align-reference=type

--add-braces
--add-one-line-braces

# Function return type is on top
--break-return-type

--convert-tabs
--max-code-length=100
--min-conditional-indent=0
--max-continuation-indent=120

--attach-closing-while

--mode=c
--lineend=linux

--preserve-date
--formatted
# No backup files
--suffix=none

# Exclude if needed
--exclude="test"
--exclude="src/libs/lang/include/lang/except.h"

--ignore-exclude-errors