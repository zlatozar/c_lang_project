May be better name is 'ext-deps'

External third party dependancies that are build raraly, build is slow
or are OS dependant, will be placed here as submoduls.

Should be builded and prepared for static linking.
NOTE: If changes are needed fork dependand library in you own reporistory.

TODO: Add one Makefile to build them all